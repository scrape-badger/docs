---
title: "Cline"
description: "Connect ScrapeBadger to Cline (VS Code AI agent) via MCP."
---

# Cline

## Setup

Click the **MCP Servers** icon in Cline's top navigation > **Configure** tab > "Configure MCP Servers":

```json
{
  "mcpServers": {
    "scrapebadger": {
      "url": "https://mcp.scrapebadger.com/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
```

## Auto-Approve Tools

To skip confirmation prompts for specific tools, add `alwaysAllow`:

```json
{
  "mcpServers": {
    "scrapebadger": {
      "url": "https://mcp.scrapebadger.com/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      },
      "alwaysAllow": ["twitter_get_trends", "twitter_search_users"]
    }
  }
}
```

## Verify

After saving, Cline automatically detects the available tools. You should see 63 ScrapeBadger tools in the MCP Servers panel.
